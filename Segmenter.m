classdef Segmenter
    % 輸出偵測到的所有bboxes[x, y, width, height]，二維陣列
    properties
        % 這裡放「資料 / 變數 / 參數」，代表這個類別知道什麼（State）
    
    end
    methods
        % 這裡放「函數 / 功能 / 動作」，代表這個類別能做什麼（Behavior）
        
    end
end